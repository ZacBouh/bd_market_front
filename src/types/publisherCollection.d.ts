type NewPublisherCollection = {
    name : string,
    publisherId : number,
    titleIds? : string,
    description? : string,
    birthDate? : string,
    deathDate? : string,
    coverImageFile? : File,
}