type UploadedImage = {
    id: number, 
    fileName: string,
    originalFileName: string | null,
    fileMimeType: string | null,
    fileSize: number,
    imageDimensions: string | null,
    imageName: string,
    url: string,
    updatedAt: string
}
